{% extends 'base.html.twig' %}

{% block title %}Hello CvController!{% endblock %}
{% block stylesheets %}
<link href='/styles/css/cv.css' rel="stylesheet" />

{% endblock %}

{% block body %}

<section class="projects">
    <h2>Mes projets</h2>
    {% for p in Project %}
    <div class="section_title">
        <li class="project_item underlined">{{ p.name }}</li>
        <li class="project_item">{{ p.date|date('d-m-Y') }}</li>
        <li class="project_item">{{ p.description }}</li>
    </div>
    {% endfor %}
</section>

<section class="stack">
    <h2>Ma stack</h2>
    {% for t in Techno %}
    <div class="section_title">
        <li class="project_item">{{ t.name }}</li>
    </div>
    {% endfor %}
</section>

{# <div class="experiences">experiences</div>
<div class="techno">techno</div>
<div class="training">training</div>
<div class="hobby">hobby</div>
#}





{% endblock %}